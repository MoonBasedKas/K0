#include <stdio.h>

int main() {
    int a = 10;
    int b = 3;
    int sum  = a + b;
    int diff = a - b;
    int prod = a * b;
    printf("sum=%d diff=%d prod=%d\n", sum, diff, prod);
    return 0;
}